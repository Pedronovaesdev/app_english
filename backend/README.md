# English Mastery OS — API

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate   # Windows
pip install -e .
uvicorn app.main:app --reload --port 8000
```

SQLite database file: `backend/data/emastery.db` (created on first run).
